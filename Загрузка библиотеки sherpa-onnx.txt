Загрузка библиотеки sherpa-onnx

Программе нужна библиотека sherpa-onnx, её нет в requirements.txt. 

Установлю:

Готово. Попробуй снова запустить start.bat. Если снова ошибка — скинь вывод.


Проблема в том, что sherpa-onnx не был в requirements.txt. Сейчас сделаю так, чтобы проект стал автономным:

Добавлю sherpa-onnx в requirements.txt
Создам setup.bat — он поставит всё автоматически
Переделаю start.bat — он будет использовать виртуальное окружение внутри проекта
Теперь запущу setup чтобы создать venv:

Готово. Вот что изменилось:

requirements.txt — добавил sherpa-onnx

start.bat — теперь использует venv внутри проекта. Если venv нет — автоматически запускает setup.bat

setup.bat — создает venv и ставит все зависимости одной командой

Как теперь работает после переустановки винды:

Ставишь Python
Заходишь в папку проекта
Запускаешь start.bat — он сам всё создаст и запустит
Попробуй запустить start.bat — должно работать.

Раз, два, три, четыре, пять.
Работает? Модель загрузилась?

Да, всё заработало.
Отлично! На будущее — теперь проект полностью автономный:

Python ставишь → запускаешь start.bat → он сам создаёт venv и ставит зависимости
Все библиотеки хранятся в папке venv внутри проекта на диске D
После переустановки винды ничего лишнего ставить не нужно
